# Frontend environment variables
# These are safe to expose to the client

# API base URL (leave empty for same-origin requests)
NEXT_PUBLIC_API_BASE_URL=

# Firebase App Check site key (optional, for bot protection)
NEXT_PUBLIC_APP_CHECK_SITE_KEY=
